# AutoCrop Test Dataset

This directory contains the AutoCrop regression test framework.

The folders are intentionally empty.

Add your own PDF test documents to:

    input/

These files are ignored by Git to avoid publishing personal documents.

Run the tests with:

    python3 tests/autocrop/run.py
